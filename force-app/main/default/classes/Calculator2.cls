public class Calculator2 {
   public decimal dynamicCalculationMethod (decimal num1, decimal num2, string method ) {
       decimal result;
       switch on method {
           when  'addition'{
               result = num1+num2;    
           }
           when else {
               
           }
       }

    }
}
